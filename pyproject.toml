[project]
name = "ansible-xen-orchestra"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "ansible>=13.5.0",
    "requests>=2.33.1",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
ignore = ["E402"]
