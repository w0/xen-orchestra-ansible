# Xen Orchestra Ansible

An ansible collection for managing resources in Xen Orchestra through the rest api.

## Development

To set up a development environment:

1. Clone the repository
2. Install dependencies: `uv sync`
